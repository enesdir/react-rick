{
	"$schema": "https://turbo.build/schema.json",
	"pipeline": {
		"type-check": {
			"outputs": ["*.tsbuildinfo"]
		},
		"build": {
			"outputs": ["dist/**", "public/**"],
			"env": ["VITE_API_URL"]
		},
		"lint": {
			"outputs": [],
			"inputs": [".eslintrc.*", "src/**"]
		}
	}
}
