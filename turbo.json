{
	"$schema": "https://turbo.build/schema.json",
	"pipeline": {
		"type-check": {
			"outputs": ["*.tsbuildinfo"]
		},
		"build": {
			"outputs": ["dist/**", "public/**"]
		},
		"lint": {
			"outputs": [],
			"inputs": [".eslintrc.*", "src/**"]
		}
	}
}
